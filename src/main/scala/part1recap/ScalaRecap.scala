package part1recap

object ScalaRecap extends App {
  /* Write Example Programs for these
   instructions vs expressions
   instruction are executed
   expressions are evaluated, converted to a value
   HOFS
   Generics
   Functions which take one function as argument and return another
   Monads : Option, Try (bind(flatmap) and apply) https://dev.to/theodesp/explain-monads-like-im-five
    Container to retrieve a value
   pattern matching
   MultiThreading fundamentals (Threads, ThreadPoolExecutor,  locking resources, synchronization, wait, notify)
   Futures
   Partial Functions
   Type Aliases
   Implicits
    -> Pass them as parameters
    -> Extending existing Classes
    -> How are implicits fetched (Scope)
    ->
  */



}
